# --- Secrets / credentials: NEVER commit to a public repo ---
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*secret*
*credential*
*service-account*.json
*serviceAccount*.json
id_rsa*

# --- System / editor cruft ---
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# --- If build tooling is ever added here ---
node_modules/
dist/
.cache/
*.log

# --- Rendered ad creatives (84MB+) ---
# Regenerate with `bash render_ads.sh`; the _ad_*.html sources ARE tracked.
# Kept out of git because this is a public Pages site: marketing/ has no leading
# underscore, so Jekyll WOULD serve every render at the live domain.
marketing/
